A package helping with reading data from cosmological (zoom-in) simulations,
including snapshots (with pygad), subfind data, ketju data and data outputted for all BHs.

Different python files include helper function for different data structures.

Install the library with command

```
$ pip install -e .
```

## TODOs
    * Add ketjugw and pygad to requirements
    
    * add numpy 2.0 as requirement